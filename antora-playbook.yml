site:
  title: Cockpit Docs
  start_page: cockpit-guide::index.adoc
content:
  sources:
  - url: https://github.com/Venefilyn/cockpit.git
    # TODO: Update when AsciiDoc PR is merged to Cockpit and also backport the docs to other branches.
    branches:
      # Include main branch
      # - main
      - doc/move-antora
    # tags: v*
    # branches: [main, rhel-*]
    # version: true
    start_path: doc/
antora:
  extensions:
  - ./extensions/cockpit-prerelease.js
ui:
  bundle:
    # TODO: Check if Fedora's Antora UI kit is better in some way
    # TODO: Add supplemental CSS and header to make it more Cockpit-like
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true
  supplemental_files: ./supplemental-ui
